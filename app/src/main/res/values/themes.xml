<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      Material3 主题：布局使用的 textAppearanceTitleLarge / textAppearanceBodyLarge
      等 M3 字体 token 只在 Theme.Material3 中定义。若用 MaterialComponents 主题，
      ?attr 解析失败会让 TextView 落回默认小字号（标题与正文一样大且很小）。
    -->
    <style name="Theme.Chameleon" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>
</resources>
